<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>ar.edu.utn.frc.tup.p4</groupId>
  <artifactId>users-service</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>users-service</name>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <spring-boot.version>4.1.1</spring-boot.version>
    <spring-cloud.version>2025.1.3</spring-cloud.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- ORDEN OBLIGATORIO: Boot PRIMERO. Si va después, el BOM de Cloud
           puede bajar spring-boot-dependencies por debajo de 4.1.1. -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-security</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-data-jpa</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-data-redis</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-thymeleaf</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-actuator</artifactId></dependency>
    <!-- Boot 4 modularizo la auto-config, igual que con Flyway mas abajo: solo
         spring-kafka trae la libreria pero NINGUNA auto-config, asi que no hay
         bean de KafkaTemplate y OutboxPoller no se puede construir. La suite no
         lo notaba porque AbstractIntegrationTest apaga el poller con
         users.outbox.poller-enabled=false; el fallo aparece recien al arrancar
         de verdad. El starter tira del modulo spring-boot-kafka. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-kafka</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
    </dependency>
    <dependency><groupId>com.nimbusds</groupId><artifactId>nimbus-jose-jwt</artifactId><version>10.0.1</version></dependency>
    <!-- RSAKey.parseFromPEMEncodedObjects() necesita BouncyCastle (JcaPEMKeyConverter
         vive en bcpkix). Sin esto, las claves PEM montadas no se pueden leer. -->
    <dependency><groupId>org.bouncycastle</groupId><artifactId>bcpkix-jdk18on</artifactId><version>1.80</version></dependency>
    <dependency><groupId>com.mysql</groupId><artifactId>mysql-connector-j</artifactId><scope>runtime</scope></dependency>
    <!-- Boot 4 modularizo la auto-config: el starter tira del modulo
         spring-boot-flyway que antes vivia en el monolito. Solo flyway-core
         ya no auto-configura nada (falla silenciosa). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-mysql</artifactId></dependency>

    <!-- Testcontainers 2.x renombro los artefactos (DEC-20 r6 sigue exigiendo
         la MISMA imagen de produccion, el nombre del jar es solo cosmético). -->
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-testcontainers</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.springframework.security</groupId><artifactId>spring-security-test</artifactId><scope>test</scope></dependency>
    <!-- Solo para poder referenciar JwtDecoder.class en el test de DEC-08.
         Sin starter de resource server NO se autoconfigura ningun decoder. -->
    <dependency><groupId>org.springframework.security</groupId><artifactId>spring-security-oauth2-jose</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.springframework.kafka</groupId><artifactId>spring-kafka-test</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.testcontainers</groupId><artifactId>testcontainers-junit-jupiter</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.testcontainers</groupId><artifactId>testcontainers-mysql</artifactId><scope>test</scope></dependency>
    <dependency><groupId>com.redis</groupId><artifactId>testcontainers-redis</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.testcontainers</groupId><artifactId>testcontainers-kafka</artifactId><scope>test</scope></dependency>
    <dependency><groupId>com.tngtech.archunit</groupId><artifactId>archunit-junit5</artifactId><version>1.3.0</version><scope>test</scope></dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        -parameters es OBLIGATORIO. Lo agrega el parent POM de Spring Boot,
        pero aca importamos el BOM en vez de heredar del parent, asi que hay
        que ponerlo a mano. Sin el, cualquier @PathVariable / @RequestParam
        sin nombre explicito tira 500 EN RUNTIME:
        "Name for argument of type [UUID] not specified".
        Los tests no lo detectan si llaman a los servicios directamente en vez
        de pasar por HTTP.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <compilerArgs>
            <arg>-parameters</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <!--
          La ejecucion `repackage` la declara el parent POM de Spring Boot.
          Como aca se importa el BOM en vez de heredar del parent, hay que
          declararla a mano: sin ella el jar queda PLANO, sin Main-Class ni
          dependencias, y el contenedor muere con
          "no main manifest attribute, in app.jar".
          `mvn test` y `mvn verify` pasan igual: solo se nota al empaquetar.
        -->
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <!--
        Sin esto los *IT NO CORREN: surefire solo toma *Test, y los criterios
        de DoD que viven en tests de integracion se saltearian en silencio.
        `mvn verify` corre unitarios + integracion; `mvn test`, solo unitarios.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <!--
            OBLIGATORIO cuando esta la ejecucion `repackage` de arriba.
            Failsafe corre por defecto contra el ARTEFACTO empaquetado, y
            repackage lo convierte en un fat jar donde las clases quedan bajo
            BOOT-INF/classes. El escaneo de paquetes de Spring no las ve y
            TODAS las IT mueren con "Unable to find a @SpringBootConfiguration
            by searching packages upwards from the test" — un mensaje que
            manda a revisar los paquetes de los tests, donde no esta el error.
            Apuntando a target/classes las IT corren contra las clases sueltas.
          -->
          <classesDirectory>${project.build.outputDirectory}</classesDirectory>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
